<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PrismSportApp.Views.TeamInfoPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:Svg="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:yummy="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    Title="Team Info"
    mc:Ignorable="d">
    <ContentPage.Content>
        <ScrollView>
            <yummy:PancakeView
                Margin="0,10,0,0"
                CornerRadius="20"
                HasShadow="True"
                HeightRequest="400"
                HorizontalOptions="Center"
                VerticalOptions="Start"
                WidthRequest="350">
                <StackLayout Spacing="20">
                    <StackLayout VerticalOptions="Start">
                        <Svg:SvgCachedImage
                            Margin="0,0,0,0"
                            Aspect="AspectFit"
                            HeightRequest="100"
                            Source="{Binding Table.Team.CrestUrl, Mode=TwoWay}"
                            VerticalOptions="Start" />
                    </StackLayout>
                    <Label Style="{StaticResource Key=TitlesTeamName}" Text="{Binding Table.Team.Name, Mode=TwoWay}" />
                    <StackLayout
                        HorizontalOptions="Center"
                        Orientation="Horizontal"
                        Spacing="30">
                        <StackLayout>
                            <Label Style="{StaticResource Key=Titles}" Text="Win" />
                            <Label Style="{StaticResource Key=Content}" Text="{Binding Table.Won, Mode=TwoWay}" />
                        </StackLayout>
                        <StackLayout>
                            <Label Style="{StaticResource Key=Titles}" Text="Draw" />
                            <Label Style="{StaticResource Key=Content}" Text="{Binding Table.Draw, Mode=TwoWay}" />
                        </StackLayout>
                        <StackLayout>
                            <Label Style="{StaticResource Key=Titles}" Text="Lost" />
                            <Label Style="{StaticResource Key=Content}" Text="{Binding Table.Lost, Mode=TwoWay}" />
                        </StackLayout>
                        <StackLayout>
                            <Label Style="{StaticResource Key=Titles}" Text="Played Games" />
                            <Label Style="{StaticResource Key=Content}" Text="{Binding Table.PlayedGames, Mode=TwoWay}" />
                        </StackLayout>
                    </StackLayout>
                    <Button
                        Command="{Binding Save}"
                        Style="{StaticResource Key=ButtonTeamInfo}"
                        Text="Add to favorites" />
                </StackLayout>
            </yummy:PancakeView>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>