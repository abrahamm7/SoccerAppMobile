<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="PrismSportApp.Views.ListLeaguesPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:d="http://xamarin.com/schemas/2014/forms/design"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:svg="clr-namespace:TinySvgHelper;assembly=TinySvgHelper"
    x:Name="LeagueList"
    Title="Leagues"
    BackgroundColor="White"
    Visual="Material"
    mc:Ignorable="d">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout Margin="0,120,0,0" Padding="15">
                <CarouselView
                    x:Name="ListLeague"
                    ItemsSource="{Binding Leagues}"
                    Style="{StaticResource ListLeagueCarousel}">
                    <CarouselView.ItemTemplate>
                        <DataTemplate>
                            <StackLayout HorizontalOptions="CenterAndExpand">
                                <Frame Style="{StaticResource ListLeagueFrames}">
                                    <StackLayout VerticalOptions="End">
                                        <StackLayout>
                                            <Image Source="{Binding Path=BindingContext.Logo, Source={x:Reference LeagueList}, Mode=TwoWay}" Style="{StaticResource ListLeagueImage}" />
                                        </StackLayout>
                                        <StackLayout HorizontalOptions="Center">
                                            <Label
                                                Style="{StaticResource ListLeagueLabel}"
                                                Text="{Binding Name}"
                                                Visual="Material" />
                                        </StackLayout>
                                    </StackLayout>
                                    <Frame.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Path=BindingContext.Tap, Source={x:Reference LeagueList}}" CommandParameter="{Binding .}" />
                                    </Frame.GestureRecognizers>
                                </Frame>
                            </StackLayout>
                        </DataTemplate>
                    </CarouselView.ItemTemplate>
                </CarouselView>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>